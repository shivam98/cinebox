<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shiv_ndroid98.cinebox">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/food"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Information" />
        <activity android:name=".RecentHindi" />
        <activity android:name=".NowPlaying" />
        <activity android:name=".people.PopularPeople" />
        <activity android:name=".spashscreen.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MovieInTheatres" />
        <activity android:name=".people.PeopleInformation" />
        <activity android:name=".About" />
        <activity android:name=".PopularMovies" />
        <activity android:name=".people.PeopleSearchActivity" />
        <activity android:name=".MovieSearch"></activity>
    </application>

</manifest>